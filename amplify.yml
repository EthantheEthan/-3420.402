version: 1
frontend:
    phases:
        build:
            commands:
                - 'npm run build'
    artifacts:
        baseDirectory: dist/apps
        files:
            - '**/*'
    cache:
        paths: []
